fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.50
pydantic>=2.6
python-dotenv>=1.0
google-genai>=1.0     # only needed if LLM_PROVIDER=gemini
anthropic>=0.40      # only needed if LLM_PROVIDER=anthropic
openai>=1.30         # only needed if LLM_PROVIDER=openai
pytest>=8.0
httpx>=0.27          # required by FastAPI's TestClient
pandas>=2.0
